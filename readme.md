# Running the Project
make sure you already installed python.
write in command line:
`pip install -r requirements.txt` for download the required library
`python -m venv venv`
`.\venv\Scripts\activate`
`uvicorn main:app --reload` for running the project
